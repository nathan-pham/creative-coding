# Creative Coding

Random experiments, from generative art to interactive components. I'm currently modernizing and updating all of the sketches here, but you can still view them under `archive`.

## Build Sketches

Use `buildSketches.js` to automatically add links to `index.html` based on the folder contents of `sketches` without manually typing them yourself.
